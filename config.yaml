# Cookie settings only — credentials are stored in the [app_users] DB table.
# AUTH_COOKIE_KEY must be set in your .env file (never commit the real key).
cookie:
  expiry_days: 0
  name: camfart_cookie
  # key is injected at runtime from the AUTH_COOKIE_KEY environment variable
